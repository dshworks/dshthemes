{
  "name": "dshthemes",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "dshthemes.com — every DeepSeek Harness theme, in its own colours. A reader of dshworks/awesome-dsh-themes.",
  "scripts": {
    "registry": "node scripts/registry.mjs",
    "enrich": "node scripts/enrich.mjs",
    "build": "node scripts/build.mjs",
    "shots": "node scripts/build.mjs && node scripts/shots.mjs && node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "playwright-core": "^1.58.0",
    "sharp": "^0.34.5",
    "wrangler": "^4.93.1"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
